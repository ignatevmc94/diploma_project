# 🛒 Shop API (Django + DRF)

Учебный backend-проект розничного интернет-магазина с поддержкой нескольких поставщиков (магазинов), корзины, оформления заказа, адресов доставки и импорта товаров из YAML.

---

## 📌 Описание проекта

Проект реализует базовую логику маркетплейса:

- **Покупатель**:
  - смотрит товары
  - добавляет товары в корзину
  - оформляет и подтверждает заказ
  - добавляет / удаляет адрес доставки (контакты)

- **Поставщик (магазин)**:
  - импортирует товары из YAML
  - может включать / выключать прием заказов
  - видит только **свои позиции в заказах**
  - меняет статус выполнения **своих товаров** в заказе

- **Администратор**:
  - получает email-уведомление о заказе (через Celery)

---
## Схема базы данных
![Схема БД](Schema_DB.png)

## 🧩 Технологии

- Python 3.x
- Django 5.2
- Django REST Framework
- Token Authentication (DRF authtoken)
- Celery + Redis
- SQLite (в учебной версии)
- Ruff (для приведения к нормам PEP8)
- GitHub Copilot Chat и ChatGPT (при написании кода)
- Postman (для ручного тестирования)

---

## 🔐 Аутентификация

Используется **Token Authentication**.

### Как передавать токен
Заголовок:

Authorization: Token <ваш_токен>


---

## 👥 Роли

### Покупатель
- обычный пользователь
- работает с товарами, корзиной, заказами, контактами

### Поставщик
- пользователь, у которого есть магазин
- имеет доступ к эндпойнтам `/supplier/...`
- видит только свои товары в заказах

---

# 📍 Endpoints

Базовый префикс API:

/api/


---

## ✅ Регистрация

### `POST /api/register/`

**Описание:** регистрация пользователя

**Body:**
```json
{
  "username": "vasya",
  "email": "vasya@mail.ru",
  "password": "12345678"
}
```

**Response:**
```json

{
  "status": "user created",
  "username": "vasya"
}
```


## ✅ Логин

### POST /api/login/
Описание: получение токена

**Body:**
```json
{
  "username": "vasya",
  "password": "12345678"
}
```
**Response:**
```json
{
  "token": "xxxxxxxxxxxxxxxxxxxx"
}
```

## 🔁 Сброс пароля
### POST /api/password-reset/

**Body:**
```json
{
  "email": "vasya@mail.ru"
}
```

**Response:**
```json
{
  "status": "password reset email sent"
}
```

## 🛍️ Товары
### 📌 Список товаров
### GET /api/products/

Описание: возвращает товары с предложениями из магазинов

**Response (пример):**

**Body:**
```json
[
  {
    "id": 1,
    "name": "iPhone XR",
    "category": {"name": "Phones"},
    "product_infos": [
      {
        "id": 10,
        "shop": "Связной",
        "price": "69990.00",
        "quantity": 3,
        "parameters": [
          {"parameter": {"name": "color"}, "value": "red"}
        ]
      }
    ]
  }
]
```

## Фильтрация
### По магазину

GET /api/products/?shop=1

### По категории
GET /api/products/?category=3

### 📌 Детали товара
GET /api/products/id/

### Пример:

GET /api/products/1/

## 🛒 Корзина

### 📌 Получить корзину

GET /api/cart/

Response:
```json
{
  "id": 12,
  "status": "cart",
  "items": [
    {"id": 5, "product_info": 10, "quantity": 2}
  ],
  "total_price": "139980.00",
  "created_at": "2026-01-25T15:00:00Z"
}
```

## ➕ Добавить товар в корзину
POST /api/cart/

**Body:**
```json
{
  "product_info": 10,
  "quantity": 1
}
```

**Response:**
```json
{
  "status": "item added"
}
```

## ⚠️ Если магазин временно не принимает заказы (is_accepting_orders=False) — будет ошибка.

## ❌ Удалить позицию из корзины
DELETE /api/cart/<item_id>/

**Пример:**

DELETE /api/cart/5/

**Response:**
```json
{
  "status": "item deleted"
}
```

## 📦 Заказы (покупатель)
### 🧾 Создать заказ из корзины
POST /api/order/create/

Описание: переводит корзину в заказ со статусом new

**Response:**
```json
{
  "status": "Order created"
}
```

### ✅ Подтвердить заказ (контакт доставки)
POST /api/order/confirm/

Можно передать contact_id:
```json
{
  "contact_id": 1
}
```
или создать контакт прямо в запросе:

```json
{
  "contact": {
    "phone": "88005553535",
    "city": "Moscow",
    "street": "Red Square",
    "house": "1",
    "apartment": "10"
  }
}
```

**Response:**
```json
{
  "status": "order confirmed"
}
```

### После подтверждения:

* заказ получает статус confirmed

* все OrderItem получают статус confirmed

* запускаются Celery-задачи:

* письмо покупателю

* письмо админу

## 📄 Список заказов покупателя
GET /api/orders/

**Response:**
```json
[
  {
    "id": 1,
    "status": "confirmed",
    "items": [
      {"id": 1, "product_info": 10, "quantity": 1}
    ],
    "total_price": "69990.00",
    "created_at": "2026-01-25T14:11:34Z"
  }
]
```

## 📄 Детали заказа покупателя
GET /api/order/(id)/

📍 Контакты доставки (адреса)

📌 Получить список контактов

GET /api/contacts/

➕ Добавить контакт

POST /api/contacts/

**Body:**
```json
{
  "phone": "123456789",
  "city": "NSK",
  "street": "Ippodrom",
  "house": "100",
  "apartment": "12"
}
```
## ✏️ Обновить контакт
PATCH /api/contacts/(id)/

## ❌ Удалить контакт
DELETE /api/contacts/(id)/

## 🏪 Поставщик (Supplier API)
Все supplier endpoints требуют:

* IsAuthenticated

* IsSupplier

## 📥 Импорт товаров из YAML
POST /api/import/
**Body:**
```json
{
  "file_path": "/home/michael/Документы/Python Projects/diploma/shop2.yaml"
}
```

**Response:**
```json
{
  "status": "import completed",
  "shop": "Shop2"
}
```

## Ограничения импорта (важно)
* импорт может делать только поставщик

* импорт разрешён только в свой магазин

* если магазин не существует — создаётся из YAML

* если YAML содержит имя чужого магазина — импорт запрещён

## 🧾 Заказы поставщика
GET /api/supplier/orders/

Описание: поставщик видит только свои товары в заказах
(если заказ содержит товары других магазинов — они скрываются)

**Response:**
```json
[
  {
    "id": 2,
    "supplier_status": "confirmed",
    "customer": "vasya",
    "customer_email": "vasya@mail.ru",
    "items": [
      {
        "id": 4,
        "shop": "Shop2",
        "product_name": "iPhone 15 Pro 256GB",
        "quantity": 1,
        "price": "129990.00",
        "total_price": 129990.0,
        "status": "confirmed"
      }
    ],
    "contact": {
      "id": 6,
      "phone": "88005553535",
      "city": "Moscow",
      "street": "red square",
      "house": "100",
      "apartment": null
    },
    "total_price": 129990.0,
    "created_at": "2026-01-25T15:16:27Z"
  }
]
```

## 🔄 Включить/выключить прием заказов
POST /api/supplier/acception/
**Body:**
```json
{
  "is_accepting_orders": true
}
```

**Response:**
```json
{
  "is_accepting_orders": true,
  "message": "supplier is now accepting orders"
}
```

## ✅ Статус выполнения заказа (для поставщика)

GET /api/supplier/orders/<order_id>/status/

Описание: возвращает статус заказа только для текущего поставщика
(считается по его OrderItem)

**Response:**
```json
{
  "order_id": 2,
  "supplier_status": "confirmed"
}
```

POST /api/supplier/orders/<order_id>/status/

Описание: поставщик обновляет статус всех своих товаров в заказе

**Body:**
```json
{
  "status": "done"
}
```

**Response:**
```json
{
  "order_id": 2,
  "supplier_status": "done",
  "updated_items": 1,
  "message": "Supplier items status updated"
}
```

## ⚠️ Важно: общий Order.status обновляется в done только если все позиции заказа стали done (включая другие магазины).
________________________________________

# 🧪 Endpoints

## Покупатель

POST /register/

POST /login/ → сохранить токен

POST /password-reset/

GET /products/

POST /cart/

GET /cart/

POST /order/create/

POST /contacts/

POST /order/confirm/

GET /orders/

## Поставщик

POST /import/ 

POST /login/ → токен поставщика

POST /supplier/acception/ → включить прием заказов

GET /supplier/orders/

GET /supplier/orders/(id)/status/

POST /supplier/orders/(id)/status/ → done

GET /supplier/orders/ → проверить supplier_status


# Проект доработан и расширен

---

## 📄 Документация API (Swagger / OpenAPI)

В проекте подключена автогенерация документации API с помощью **drf‑spectacular**.

### Реализовано:
- Swagger UI
- OpenAPI‑схема
- docstring‑описания всех API View

### Доступ:

- `/api/schema/` — OpenAPI schema
- `/api/docs/` — Swagger UI

📌 **Пояснения**  
Docstring активно используются в представлениях для повышения качества автогенерируемой схемы.

---

## 🚦 Ограничение запросов (DRF Throttling)

В проекте реализован базовый **DRF throttling**:

- `UserRateThrottle`
- лимит: **5 запросов в минуту на пользователя**

### Проверка:
- реализован автотест, подтверждающий возврат HTTP 429 при превышении лимита

📌 **Пояснения**  
Throttling реализован и проверен через `TestCase`, что позволяет увидеть реальное поведение лимитов.

---

## 🔐 Социальная авторизация

Подключена социальная авторизация с использованием **social‑auth‑app‑django**:

- Google OAuth2
- GitHub OAuth2

📌 Реализована минимальная конфигурация:
- backends подключены
- маршруты `/auth/…` доступны
- ключи выносятся в переменные окружения

📌 **Пояснения**  
В учебной версии используются заглушки OAuth‑ключей, структура готова для production‑настроек.

---

## 🛠 Административная панель

Админка Django улучшена с помощью **django‑baton**:

- современный интерфейс
- боковая навигация
- улучшенный UX для управления моделями

📌 **Пояснения**  
Использование сторонней админ‑темы повышает удобство сопровождения проекта.

---

## 🖼 Аватары пользователей и изображения товаров

Реализована загрузка изображений:

- аватары пользователей
- изображения товаров

Используется библиотека **easy‑thumbnails**.

### Особенности:
- изображения хранятся в `MEDIA_ROOT`
- миниатюры создаются и кэшируются

📌 Загрузка аватара осуществляется через **административную панель**.

---

## ⚙ Асинхронная генерация миниатюр (Celery)

Генерация миниатюр изображений товаров выполняется **асинхронно**:

- при сохранении товара в админке
- Celery‑задача создаёт миниатюру в фоне

📌 **Пояснения**  
Асинхронная обработка изображений ускоряет отклик API и снижает нагрузку на основной поток.

---

## 🐞 Перехват ошибок (Sentry)

В проект подключён **Sentry** для централизованного перехвата ошибок.

### Реализовано:
- базовая интеграция Sentry SDK
- тестовый endpoint `/api/sentry-debug/`, намеренно выбрасывающий исключение

📌 **Пояснения**  
Тестовый endpoint используется для демонстрации корректной работы error‑tracking.

---

## 🚀 Кэширование запросов БД (Redis + django‑cachalot)

Для ускорения работы приложения реализовано кеширование ORM‑запросов:

- Redis
- django‑cachalot

### Особенности:
- повторные ORM‑запросы не обращаются к БД
- особенно эффективно для списков товаров и заказов

📌 **Пояснения**  
Используется прозрачное кеширование без изменения существующего кода views.

---

## 🔍 Анализ производительности (django‑silk)

Для анализа «тяжёлых» запросов подключён **django‑silk**.

### Использование:
- профилирование `SupplierOrderListView`
- анализ количества SQL‑запросов
- проверка отсутствия N+1 проблемы

📌 **Пояснения**  
Silk использовался для анализа, а не для преждевременной оптимизации кода.

---

## ⚠ Учебные упрощения проекта

Проект является учебным, поэтому:

- используется упрощённая настройка безопасности
- OAuth‑ключи заданы как заглушки
- Sentry используется в демонстрационном режиме
- Redis и Celery настроены локально

📌 **Как было бы в production**:
- все секреты выносятся в `.env`
- отдельные Redis‑инстансы
- полноценная система логирования и мониторинга

---